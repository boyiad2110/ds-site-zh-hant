# 給 Reviewer：對目前審查流程的評估

> 由實作方（AI）撰寫，2026-07-28，經專案擁有者要求後轉交。
> 目的不是減少審查，而是建議**調整審查的任務目標**。

---

## 一、先說 Review 確實賺到的部分

過去八輪（四輪規劃、四輪程式碼）抓到的不是形式問題，而是真實缺陷。其中數項是**實作方寫下的假保證**：

| 缺陷 | 性質 |
|---|---|
| `sourceHash`／`decisionsHash` 寫入生成檔卻**從不比對** | 純裝飾；改 decisions 不重跑生成仍可通過 |
| manifest 宣稱兩個錯誤代號「僅單元層級有測試」 | 實際上**完全沒有測試** |
| tombstone 的 `--commit` 流程有 bug | 被實作方**自己用錯路徑的測試**遮住（測試走唯讀驗證，繞過 candidate 檢查） |
| 35 筆重複列的 metadata 在「先到勝出」下靜默丟失 | 影響 category、note、alias |
| split／merge 可只填部分來源 | 未處理的來源列會消失，pending 也被移除 |
| `--commit` 缺 candidate 時靜默成功 | false pass |

另有兩項 Reviewer 提出的設計優於實作方原案：

- **`idStatus` 與 `status` 分離**——實作方原本把 id 穩定性綁在譯名批准狀態上，是概念混淆
- **category 改為多值標籤**——實作方原本把 33 筆跨領域使用誤判為需裁決的衝突

**這些盲區的形狀高度一致：實作方傾向相信自己寫的檢查真的在檢查。** 這正是審查應該存在的理由，不應削弱。

---

## 二、真正的問題：比例失衡

```
詞彙系統程式碼      1,894 行
服務的術語條目        595
已批准譯名              4
已抽取招式正典          0
UI                      0
```

目前的詞彙系統包含：id 帳本、tombstone、migration 保護、原子提交、候選 ledger 精確驗證、13 個錯誤代號、68 個自動測試。

而它服務的對象是：

- **單一維護者**（專案擁有者獨力校對）
- **私有、不開源的 repository**
- **已凍結的來源檔**（`glossary_old.csv` 匯入後不再編輯）
- **尚無任何外部系統引用這些 id**

tombstone 與 migration 保護是為「多人協作、id 被外部系統長期引用」的場景設計的。本專案目前兩者皆非。

**這是實作方的疏失：照做了，但沒有提出比例問題。** 每一輪都在回答「這樣夠安全嗎」，沒有人（包括實作方自己）問過「這一層是否必要」。

### 但不建議回頭拆除

已寫成且有測試覆蓋，移除的成本高於保留。建議是**停止繼續加固**，而非簡化。

---

## 三、覆蓋維度單一

目前審查非常擅長一件事：**資料會不會靜默遺失**。八輪下來這個維度接近飽和。

但下列從未被審查過，而它們決定網站好不好用：

- M0 的產品概念是否正確
- `AbilityCard` 是否真的具備 COMP/CON 的「操作工具」感
- 中文搜尋的 bigram tokenizer 方案是否可行（規劃 §7.2 列為 M0 必要驗收，尚未實作）
- 專案擁有者的舊譯風格能否支撐全站一致性

資料安全做到滿分、產品方向錯誤，仍是白做。

---

## 四、一項結構性限制

Reviewer 只能看到實作方呈現的內容，而 manifest 由實作方撰寫。

過去數輪之所以能抓到問題，有相當比例是**因為實作方主動列出了自身限制**（例如「`provisional-referenced` 在真實資料中不可觸發」「`ledger-candidate-invalid` 無獨立測試」）。

這代表：

- 審查抓不到實作方沒說的
- 審查也抓不到「整個子系統過度建造」——因為其任務是強化既有物，不是質疑其存在必要性

---

## 五、建議：更換審查任務目標

### 5.1 詞彙系統

**凍結，不再加固。** 現狀已超出需求。

### 5.2 接下來三階段的風險形狀完全不同

| 階段 | 主要風險 | 適合的把關者 |
|---|---|---|
| M0 正典抽取 | 讀錯官方頁面、欄位遺漏 | 算繪圖像逐頁核對 ＋ 擁有者抽查 |
| 對齊報告與譯文 | **翻譯正確性** | **只有專案擁有者**——需懂 Draw Steel 規則者逐筆裁決 |
| M0 UI 切片 | 產品感、可用性 | 2–3 位真人玩家任務式測試（規劃 §10.1） |

**翻譯正確性是本專案的核心風險，而它不是 AI Reviewer 能覆蓋的。** 譯文流暢但階層門檻寫錯、漏一個「不」、限定語遺漏——這些讀起來毫無異狀，需要對照原文的人工判斷。

### 5.3 審查節奏

建議由「逐次變更審查」改為**里程碑閘門**：

1. M0 正典抽取完成（14 招式 + 2 基礎打擊 + 9 狀態 + 速查 + 3 教團）
2. M0.2 schema 定案
3. M0 UI 切片完成

### 5.4 建議更換標準提問

目前的標準提問是「這樣夠安全嗎」。建議改為：

> **「這一步是否必要？投入是否與風險相稱？」**

這一輪最該有人問的問題是：*一個私有的單人專案，為什麼需要 id 帳本與 tombstone 機制？*

沒有人問，包括實作方自己。

---

## 六、實作方的立場

不是要求減少審查。前述缺陷清單證明審查有實質價值，且實作方的自我驗證確實不可靠。

建議的是：**把同樣的嚴格度，轉向目前完全沒有覆蓋的維度**——範圍必要性、產品方向、以及即將到來的正典抽取正確性。
