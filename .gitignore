# ─────────────────────────────────────────────────────────────
# 授權素材 —— 絕對不得進版控
# 官方 PDF 與字型為 MCDM 授權素材，不得散布。
# 專案僅在本機持有並用於抽取，抽取結果的範圍另見 README 的授權說明。
# ─────────────────────────────────────────────────────────────
sources/official/
sources/drawsteelglyphs/
*.pdf
*.otf
*.ttf
*.woff
*.woff2

# 由官方 PDF 算繪出的頁面圖像 —— 等同於重製官方版面，一律排除
docs/**/evidence/
*-150dpi.png
*-300dpi.png
docs/alignment/evidence/
docs/samples/evidence/

# ─────────────────────────────────────────────────────────────
# 原始 Notion 匯出 —— 擁有者的舊譯語料，體積大且含私人工作紀錄
# 排除後 scripts/report-term-frequency.mjs 無法執行（唯讀報告工具，不影響主流程）
# ─────────────────────────────────────────────────────────────
sources/notion-export/

# ─────────────────────────────────────────────────────────────
# 抽取與算繪的中間檔
# ─────────────────────────────────────────────────────────────
out/
tmp/
scratch/
scratchpad/
*.tmp
p[0-9]*.txt
L[0-9]*.txt
rr[0-9]*.txt
h[0-9]*.txt
crop-*.png
*-crop.png

# ─────────────────────────────────────────────────────────────
# 依賴與建置產物
# ─────────────────────────────────────────────────────────────
node_modules/
dist/
build/
.next/
.cache/
coverage/
*.tsbuildinfo

# ─────────────────────────────────────────────────────────────
# 本機／工具狀態
# ─────────────────────────────────────────────────────────────
.local-context/
.claude/settings.local.json
.claude/*.local.json
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# ─────────────────────────────────────────────────────────────
# 機密 —— 目前掃描無命中，此處為預防
# ─────────────────────────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
secrets.json
credentials.json
*.credentials

# ─────────────────────────────────────────────────────────────
# 執行期生成的暫時性檔案
# id-ledger.candidate.json 是 validate-terms --commit 前的候選檔，
# 提升後即刪除；留在版控會造成假衝突。
# ─────────────────────────────────────────────────────────────
data/id-ledger.candidate.json
data/id-ledger.json.tmp
npm-debug.log*
*.log

# ─────────────────────────────────────────────────────────────
# Review 用的 patch 檔
# 這些是「專案尚未納入版控」時期為了外部 review 而產生的 new-file diff
# （見 review-2026-07-28-manifest.md）。所涵蓋的檔案全部仍存在於工作目錄，
# 且已被版控以更新的狀態收錄 —— 保留只會重複保存過期內容。
# 稽核紀錄由 review-*-manifest.md 承擔，patch 本身是傳輸手段，不是紀錄。
# ─────────────────────────────────────────────────────────────
*.patch
*.diff
